# Optional: server-side fallback API key for local dev convenience.
# When set, leaving the API key field empty in the browser will use this key.
# In production, leave this unset so each user must provide their own key.
GEMINI_API_KEY=
